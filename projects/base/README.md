# Base CUDA project

Minimal Linux x86-64 PyTorch 2.4.1 + CUDA 12.4 project for the Kaggle template.
It is independently usable with `pixi install --locked`, `pixi run setup`, and
`pixi run check-gpu`.
